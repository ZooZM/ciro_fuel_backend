/** spec 005 T116 — mirrors the app's support-topic categories (mobile's
 * `support_topics.dart`), plus the generic default `support_order_problem_card.dart`
 * submits for an order-attached report. */
export enum SupportTopic {
  ORDER_ISSUE = 'ORDER_ISSUE',
  PAYMENT = 'PAYMENT',
  ACCOUNT = 'ACCOUNT',
  OTHER = 'OTHER',
}
